#include <iostream>
#include <cstdlib>

using namespace std;

int main(){

    /*
        SYNTAX

		Declaration:
			<array_type (int, float, etc.)> <array_name>[<array_size>];
			
		Atribution:
			<array_name>[<array_position>] = <value>;
			
		Display (specific position / value):
			cout << <array_name>[<array_position>];
			
		Display (all positions / values):
			for (i = 0; i < sizeof(<array_name>); i++){
				cout << <array_name>[<i>];
			}
	*/
	
	system("pause");
	return 0;
}
